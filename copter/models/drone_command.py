from django.db import models

class DroneCommand:

	# read and write data
	#  is_attempt_connect, connection_port, connection_baud_rate, current_heaidng_waypoint
	#   is_attempt_mission, is_attempt_arm, is_attempt_disarm,

	pass